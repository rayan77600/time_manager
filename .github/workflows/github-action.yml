name: SonarQube Scan (Python)

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  sonar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r backend/requirements.txt
          python -m pip install bandit

      - name: Run tests (optional)
        working-directory: backend
        run: |
          python -m pytest --maxfail=1 --disable-warnings -q || true
      
      - name: Run Bandit Security Scan
        working-directory: backend
        run: |
          echo "🔍 Running Bandit security scan..."
          bandit -r app/ -f json -o bandit-report.json || echo "⚠️ Bandit finished with issues, but not blocking build."

      - name: Upload Bandit report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-security-report
          path: backend/bandit-report.json
      
      - name: Send Bandit JSON report to Discord
        if: always()
        run: |
           curl -F "payload_json={\"content\": \"🛡️ Rapport Bandit (analyse de sécurité Python)\"}" \
           -F "file=@backend/bandit-report.json" \
           ${{ secrets.DISCORD_WEBHOOK_URL }}


      - name: Run SonarQube Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=drownedmilan
            -Dsonar.projectKey=DrownedMilan_time_manager
            -Dsonar.python.coverage.reportPaths=coverage.xml
            -Dsonar.sources=backend/app

      - name: Send Discord Notification             # DISCORD NOTIF
        if: always()  # s’exécute même si le build échoue
        run: |
          STATUS="✅ Build succeeded"
          if [ "${{ job.status }}" != "success" ]; then
            STATUS="❌ Build failed"
          fi
          BRANCH="${GITHUB_REF_NAME}"
          MESSAGE="🚀 New push on branch **$BRANCH** by **${{ github.actor }}**\nStatus: $STATUS\n🔗 [View run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          curl -H "Content-Type: application/json" \
               -d "{\"content\": \"$MESSAGE\"}" \
               ${{ secrets.DISCORD_WEBHOOK_URL }}

  trivy-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        run: docker build -t time_manager-backend ./backend

      - name: Build frontend image
        run: docker build -t time_manager-frontend ./frontend

      - name: Run Trivy scans
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: |
            time_manager-backend
            time_manager-frontend
            postgres:17-bookworm
            nginx:1.28.0-alpine3.21
            quay.io/keycloak/keycloak:26.4.0
          format: 'json'
          output: 'trivy-multi-report.json'

      - name: Upload Trivy report artifact
        uses: actions/upload-artifact@v4
        with:
          name: trivy-docker-report
          path: trivy-multi-report.json

      - name: Send report to Discord
        if: always()
        run: |
          curl -F "payload_json={\"content\": \"🧩 Rapport Trivy multi-images (scan Docker)\"}" \
               -F "file=@trivy-multi-report.json" \
               ${{ secrets.DISCORD_WEBHOOK_URL }}